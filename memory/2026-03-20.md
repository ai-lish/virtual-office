# 2026-03-20 工作日誌

## Discord 設定 Bug 修復

**問題：** Discord groupPolicy 有層疊設定 bug
- 外層設定：`groupPolicy: allowlist`
- 但 `accounts.default.groupPolicy: open` 覆蓋咗外層設定

**解決：** 將 `accounts.default.groupPolicy` 改為 `allowlist`

## Sandbox 設定

- Sandbox mode 應該設喺 `agents.defaults.sandbox.mode`，唔係 root
- Valid 值：`off`, `non-main` (default)
- Root level 既 `sandbox` key 會被 doctor 移除

## Discord Status 誤導

- Status 顯示 "disconnected" 但 bot 正常運作
- 原因：streaming mode 預設 off
- 改為 `streaming: partial` 可以改善 real-time push

## Workspace 重組

- 將所有 agents 由 `~/.openclaw/agents/` 搬去 `~/.openclaw/workspace/agents/`
- Config 入面既 workspace path 已更新

## 重要 Config 設定

```json
{
  "channels": {
    "discord": {
      "groupPolicy": "allowlist",
      "streaming": "partial",
      "dmPolicy": "pairing"
    },
    "telegram": {
      "groupPolicy": "allowlist",
      "groupAllowFrom": ["8707814594", "676700807323844649"],
      "streaming": "off"
    }
  },
  "agents": {
    "defaults": {
      "sandbox": {"mode": "off"}
    }
  }
}
```

## 待解決問題

- Discord real-time push (用 fetch 取代 push) - ✅ 已解決
- 5個 Discord bots 只保留咗 1個 (Add bot) - ✅ 已解決

## Subagents Setup (14:00)

- 所有4個agents已setup喺 `~/.openclaw/workspace/agents/`
- IDENTITY.md 已填寫：書記/師弟/小詩/T仔
- Subagent spawning work喇！
- Discord mention可以spawn agents

## Files Updated

- IDENTITY.md: 加入projects + roles
- SOUL.md: 加入subagent management原則
- TOOLS.md: 加入projects URLs + cron jobs
- AGENTS.md: 加入spawn workflow
